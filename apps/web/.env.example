# Backend API base URL.
# Default points at the deployed Cloud Run service so the frontend works
# without running the backend locally.
# For local backend development, set this to http://localhost:3000
VITE_API_BASE_URL=https://cleo-device-api-623878673471.asia-east1.run.app
